# Menchmark — https://menchmark.app
#
# Everything here is public and free, so crawling is open. The only reason this
# file exists is to point crawlers at the sitemap: before it, a request for
# /robots.txt was answered by Cloudflare's managed Content Signals file, which
# is entirely comments — no User-agent line, no Sitemap line, nothing to follow.

User-agent: *
Allow: /

Sitemap: https://menchmark.app/sitemap.xml
