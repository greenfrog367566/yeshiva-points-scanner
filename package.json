{
  "name": "menchmark-firebase-tooling",
  "private": true,
  "description": "Dev-only tooling for the Firebase rebuild's security-rules and Cloud Function test suites. app.html itself stays a single dependency-free file — this package.json exists only so CI and local sessions can run these against the emulator; see CLAUDE.md rule 3.",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.24.1"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --project menchmark-rules-test --only firestore \"node firestore.rules.test.js\"",
    "test:functions": "firebase emulators:exec --project menchmark-rules-test --only firestore,auth \"node functions/provisionRebbi.test.js\""
  }
}
