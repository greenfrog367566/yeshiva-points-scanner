{
  "name": "menchmark-firebase-tooling",
  "private": true,
  "description": "Dev-only tooling for the Firebase rebuild's security-rules test suite. app.html itself stays a single dependency-free file — this package.json exists only so CI and local sessions can run firestore.rules.test.js against the emulator; see CLAUDE.md rule 3.",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.24.1"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --project menchmark-rules-test --only firestore \"node firestore.rules.test.js\""
  }
}
